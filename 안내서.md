# Cairn 안내서 — 완전 처음이라도 괜찮아요

> 🌐 **English**: [Getting Started](GETTING-STARTED.en.md) · [README.md](README.md)

이 문서는 프로그래밍이나 plugin이 처음인 분도 따라올 수 있게 썼습니다. 천천히 읽으세요.

---

## 1. Cairn이 도대체 뭔가요?

**한 줄 비유**: 등산로의 *돌탑(cairn)*. 앞선 사람이 길에 돌을 쌓아두면, 뒤따라오는 사람이
길을 잃지 않죠. Cairn은 AI와의 작업에서 그 돌탑 역할을 합니다.

AI(Claude·Codex)와 대화하다 보면 두 가지 답답함이 생깁니다.

1. **세션이 끊기면 맥락이 날아간다.** 어제 한참 얘기한 내용을 오늘 AI는 기억 못 합니다.
2. **찾아본 자료가 흩어진다.** 웹에서 찾은 좋은 문서를 그때그때 정리 안 하면 사라집니다.

Cairn은 이 둘을 해결합니다.

- 세션이 끝날 때 **"오늘 뭐 했는지"를 메모로 남기고**, 다음 세션 시작 때 **그 메모를 다시 보여줍니다.**
- 웹에서 자료를 볼 때마다 **"이거 정리해둘까요?" 후보로 모아두고**, 당신이 원할 때 정리합니다.

그리고 이 모든 기록은 **그냥 markdown 파일 + git**입니다. 특별한 데이터베이스도, 인터넷
연결도 필요 없어요. 당신 폴더 안의 평범한 텍스트 파일입니다. 언제든 직접 열어 읽고 고칠 수 있죠.

---

## 2. 핵심 개념 3가지만

| 용어 | 쉬운 설명 |
|---|---|
| **Vault (금고)** | 모든 기록이 저장되는 폴더. 보통 프로젝트 안 `.cairn/` 폴더. 당신 소유입니다. |
| **hook (자동 동작)** | 특정 순간(세션 시작/종료, 웹 검색 후)에 Cairn이 자동으로 실행하는 작은 스크립트. |
| **skill (`/cairn:setup`)** | 당신이 직접 부르는 명령. Vault(`.cairn/`)와 설정 파일을 처음 한 번 만들어줍니다. |
| **skill (`/cairn:ingest`)** | 당신이 직접 부르는 명령. 모아둔 후보를 실제 기록으로 "흡수"합니다. |

**중요한 약속**: Cairn은 당신 허락 없이 기록을 *쌓지 않습니다*. 자동 동작은 "후보 제안"까지만.
실제로 무언가를 저장하는 건 항상 당신이 `/cairn:ingest`로 승인한 뒤입니다.

---

## 3. 설치하기

### Claude Code에서 쓰는 경우

#### 준비물 확인
- **Claude Code**가 설치돼 있어야 합니다.
- **Python 3**가 있어야 합니다. 터미널에서 확인:
  ```bash
  python3 --version
  ```
  버전 숫자(예: `Python 3.11.x`)가 나오면 OK.

#### 설치 3단계

Claude Code 안에서 아래를 입력하세요(맨 앞 `/`까지 그대로).

```
/plugin marketplace add https://github.com/moonisi/cairn.git
```
→ "이 폴더를 plugin 보관소로 등록" 한다는 뜻입니다.

```
/plugin install cairn@cairn
```
→ Cairn을 실제로 켭니다.

설치 중 **"이 hook을 신뢰하겠냐"**고 물으면, 내용을 확인하고 동의하세요. (hook은 당신
컴퓨터에서 코드를 실행하므로 한 번 확인을 거칩니다 — 정상입니다.)

```
/cairn:setup
```
→ Cairn을 쓸 프로젝트에서 **딱 한 번** 실행합니다. Vault(`.cairn/` 폴더)와 설정 파일
(`cairn.config.json`)을 만들어줍니다. 내용을 보여주고 "이대로 만들까요?" 물으니 확인하고
승인하세요. **이 설정 파일이 없으면 hook이 돌아도 기록이 안 남습니다** — 그래서 처음 한 번은
꼭 필요합니다.

### Codex에서 쓰는 경우

Codex에서는 plugin을 설치해도 hook이 자동으로 연결되지 않습니다. 대신 Codex plugin으로
`/cairn:setup`·`/cairn:ingest` skill을 쓰고, hook은 프로젝트마다 `.codex/hooks.json` adapter를
한 번 복사해서 연결합니다.

먼저 Cairn을 고정 경로에 받습니다. 이 경로가 hook adapter가 참조할 `<CAIRN_ROOT_ABS>`입니다.

```bash
git clone https://github.com/moonisi/cairn.git ~/cairn
CAIRN_ROOT="$HOME/cairn"
```

Cairn을 쓰려는 프로젝트 루트에서 adapter를 복사합니다.

```bash
mkdir -p .codex
cp "$CAIRN_ROOT/adapters/codex/hooks.json.example" .codex/hooks.json
CAIRN_ROOT_ABS="$(cd "$CAIRN_ROOT" && pwd)"
sed -i "s#<CAIRN_ROOT_ABS>#$CAIRN_ROOT_ABS#g" .codex/hooks.json
```

그 다음 Codex에서 `/cairn:setup`을 실행해 `.cairn/cairn.config.json`을 만듭니다. 이 skill은
config 초안을 보여주고 승인받은 뒤에만 파일을 씁니다. Codex plugin을 아직 설치하지 않았거나
`/cairn:setup`이 보이지 않으면 [README.md](README.md)의 Codex 절을 보고 config를 수동으로
작성할 수 있습니다.

마지막으로 Codex에서 `/hooks`를 열어 `SessionStart`와 `Stop` hook 2개를 검토하고 trust하세요.
첫 응답이 끝난 뒤 `.cairn/sessions/hot.md`와 `session-state.md`가 생기면 정상입니다.

---

## 4. 잘 됐는지 확인하기

### 가장 쉬운 확인 — self-check 돌려보기

터미널에서:

```bash
cd ~/projects/cairn
python3 core/_selftest.py
```

맨 아래에 `=== 195 passed, 0 failed ===`가 나오면 코어가 멀쩡하다는 뜻입니다.

### 실제 동작 확인

1. 먼저 그 프로젝트에서 `/cairn:setup`을 한 번 실행해 설정 파일을 만들어 둡니다(위 설치 단계).
   이게 없으면 hook이 돌아도 기록이 안 남습니다.
2. 같은 프로젝트에서 Claude Code 또는 Codex 세션을 새로 시작합니다.
3. 웹 검색이나 페이지 fetch를 한 번 해보고, 세션을 끝냅니다(`/exit` 등).
4. 그 프로젝트 폴더의 `.cairn/sessions/` 안에 `hot.md`·`session-state.md` 파일이
   만들어졌는지 봅니다. 생겼다면 hook이 잘 도는 겁니다. (이 파일들은 **첫 세션을 끝낸 뒤**
   생기니, setup 직후엔 비어 있어도 정상입니다.)

---

## 5. 모아둔 자료 정리하기 — `/cairn:ingest`

웹에서 자료를 보고 나면 Cairn이 그걸 "후보"로 모아둡니다. 정리하고 싶을 때:

```
/cairn:ingest
```

그러면 Cairn이:
1. 모아둔 후보 목록을 보여줍니다.
2. 어떤 걸 정리할지 고르게 합니다(한 번에 하나씩 권장).
3. 그 내용을 정리한 **초안(draft)**을 보여주고 **"이대로 저장할까요?"** 묻습니다.
4. 당신이 승인하면 그제야 Vault에 page로 저장합니다.

승인 전에는 절대 저장하지 않으니 안심하세요.

> 참고: 단순 *검색(WebSearch)*은 출처가 아니라서 흡수 대상이 아닙니다. 실제로 *열어본
> 페이지(WebFetch)*만 정리 후보가 됩니다.

---

## 6. 자주 묻는 질문

**Q. 기록은 어디 저장되나요?**
A. 프로젝트 폴더 안 `.cairn/` 입니다. 전부 markdown 파일이라 메모장으로도 열립니다.

**Q. 인터넷이 끊겨도 되나요?**
A. 네. Cairn 자체는 외부 서버나 데이터베이스를 전혀 안 씁니다(zero 의존).

**Q. 회사 컴퓨터(관리 정책)에서 hook이 안 먹어요.**
A. 관리자가 `allowManagedHooksOnly`를 켜둔 경우입니다. 이러면 plugin뿐 아니라 내가 직접
   넣은 hook까지 전부 막힙니다(standalone 우회 불가). **유일한 해법은 조직 관리자에게
   요청해 `cairn@cairn`를 managed 설정의 `enabledPlugins`에 force-enable** 받는 것입니다.

**Q. Codex에서도 되나요?**
A. 됩니다. 다만 현재 운영 기본값은 plugin-bundled hook이 아니라 repo-local `.codex/hooks.json`
   adapter입니다. `adapters/codex/hooks.json.example`를 소비자 repo의 `.codex/hooks.json`로 복사하고
   `<CAIRN_ROOT_ABS>`를 이 Cairn repo의 절대경로로 바꿔 사용하세요. `.cairn/cairn.config.json`도
   먼저 만들어야 handoff draft가 생성됩니다. Codex plugin을 설치했다면 `/cairn:setup`으로 config를
   만들 수 있고, `/cairn:ingest`도 사용할 수 있습니다. 단, plugin은 hook을 포함하지 않습니다.
   adapter 설치 뒤 Codex에서 `/hooks` review 또는 `~/.codex/config.toml`의 trust 기록을 확인하세요
   (README "Codex" 절 참고).

**Q. 새 버전이 나오면 어떻게 업데이트하나요?**
A. Claude Code에서 두 줄이면 됩니다.
   ```
   /plugin marketplace update cairn
   /plugin update cairn@cairn
   ```
   가끔 명령 이름이 바뀌는 큰 변경(예: 옛 `/cairn:init` → `/cairn:setup`) 뒤에는 옛 버전이
   캐시에 남아 헷갈릴 수 있어요. 그럴 땐 `/plugin uninstall cairn@cairn` 후 다시 설치하면 깔끔합니다.

**Q. 기록을 직접 손으로 고쳐도 되나요?**
A. 됩니다. 다만 형식이 깨지면 `python3 -c`로 lint를 돌려 확인하세요. 그냥 markdown이니
   부담 갖지 마세요.

---

막히는 부분이 있으면 [README.md](README.md)의 해당 절을 함께 보세요.
